# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# Parsed markdown (reproducible via process.py). Anchored to the repo root so
# it does not also hide site/js/pages/.
/pages/

# Embeddings (unused in final visualization)
embed.py
embeddings.json

# Large generated files (reproducible via pipeline)
data/esco_occupations.json
data/esco_skills.json
data/site_data.json
data/skill_scores.json
data/skill_score_comparison.json
data/occupation_scores.json
data/occupation_scores_typesafe.json
data/site_data_typesafe.json
data/esco/*.zip

# Scoring log
scoring.log

# macOS
.DS_Store

# Dolt database files (added by bd init)
.dolt/
*.db

# bv (beads viewer) local config and caches
.bv/

# Test artefacts
.coverage
# SonarQube scanner work directory and the coverage reports made for it
.scannerwork/
/sonar-reports/
htmlcov/
.pytest_cache/
